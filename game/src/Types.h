#pragma once
#include <cstdint>

enum BuildingType : uint32_t
{
	BUILDING_TD,
	BUILDING_TYPE_COUNT
};

enum ProjectileType : uint32_t
{
	PROJECTILE_RIFLE,
	PROJECTILE_SHOTGUN,
	PROJECTILE_GRENADE,
	PROJECTILE_TYPE_COUNT
};
